<table class="table">
    <thead>
        <tr>
            <th>Symbol</th>
            <th>Company</th>
            <th>Price</th>
            <th>Change (%)</th>
            <th></th>
            <th></th>
        </tr>
    </thead>
         {{#each results}}
                <tr>
                <td>{{this.data.symbol}}</td>
                <td>{{this.data.companyName}}</td>
                <td>{{this.data.latestPrice}}</td>
                <td><span style="color:{{#if this.data.changeGreen}}green;{{else}}red;{{/if}}">               
                    {{this.data.changePercent}}%</span></td>
                <td>
                    <form action="/buy" method="POST">
                    <input type="hidden" name="price" value="{{this.data.latestPrice}}">
                    <input type="hidden" name="symbol" value="{{this.data.symbol}}">
                    <input type="number" name="shares" value="0">
                    <button type="submit" class="btn btn-primary">BUY</button>
                    </form>
                </td>
                {{#if this.data.notFavorite}}
                    <td>    
                        <form action="/favorites" method="POST">
                        <label for="favourite" >
                        <input type="hidden" name="symbol" value="{{this.data.symbol}}">
                        <input type="hidden" name="remove" value='notRemove'>
                        <i class="glyphicon glyphicon-star-empty"></i>
                        <i class="glyphicon glyphicon-star"></i>
                        <button type="submit" class="btn btn-primary">ADD TO FAVOURITE</button>
                        </form>
                        </label>
                    </td> 
                {{/if}}
                </tr>
        {{/each}}
        
</table>
